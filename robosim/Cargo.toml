[package]
name = "robosim"
version = "0.1.0"
edition = "2021"

[dependencies]
robowire = { path = "../robowire" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = { version = "1", features = ["float_roundtrip"] }
